<!-- DEBUG: voyage.njk utilisé -->
<body style="font-family: sans-serif; padding: 2rem;">

<p style="color: red;">⚠️ Layout voyage.njk activé</p>

<h1>TEST voyage.njk</h1>

<article class="voyage-etape">
  <h2>{{ etape.data.title }}</h2>

  <div class="content">
    {{ etape.templateContent | safe }}
  </div>

  <nav class="etape-nav" style="margin-top: 2rem;">
    {% if pagination.previous %}
      <a href="{{ pagination.previous.url }}">← {{ pagination.previous.data.title }}</a>
    {% endif %}
    {% if pagination.next %}
      <a href="{{ pagination.next.url }}" style="margin-left: 1rem;">{{ pagination.next.data.title }} →</a>
    {% endif %}
  </nav>
</article>

</body>
<!-- FIN voyage.njk -->
