---
title: Debug collections Eleventy
layout: layouts/base.njk
permalink: /fr/debug/collections/
---

<h1>🛠 Debug des collections Eleventy</h1>

{% for collectionName in collections | keys %}
  <h2>{{ collectionName }}</h2>
  <p>Nombre d’items : {{ collections[collectionName].length }}</p>

  <ul>
    {% for item in collections[collectionName] %}
      <li>
        <code>{{ item.inputPath }}</code><br />
        URL : {{ item.url }}<br />
        {% if item.outputPath %}
          Output : {{ item.outputPath }}
        {% endif %}
      </li>
    {% endfor %}
  </ul>
{% endfor %}

